import 'dart:async';

import 'package:http/http.dart' as http;

class Api {

  /*
  static Future<http.Response> getOntology(String ontology) async {
    final String url = '${Url.api}/demo/ontology/$ontology';
    final Map<String, String> h = {...?_headers};
    print('getOntology: ${url} / ${h}');
    return http.get(url, headers: h);
  }
  */
}
